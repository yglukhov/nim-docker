
set -e
echo "DOCKER: $DOCKER_USER"
cat ~/.docker/config.json
docker logout
docker login "--password=$DOCKER_PASS" "--username=$DOCKER_USER"
cat ~/.docker/config.json
$(dirname $0)/build.sh $*
docker logout

